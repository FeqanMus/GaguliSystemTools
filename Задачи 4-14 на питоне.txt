#4
print("\nЗадание 4")
print( int(5.7),', ', float(-5.7), ', ',int(float(3**39)))

#5
print("\nЗадание 5")
s = input("Введите имя вользователя: ") #ввод с клавиатуры строковое значение
print("Добро пожаловать ", s) #вывод введеного значения

#6
print("\nЗадание 6")
x = int(input("Часов в пути: ")) #ввод с клавиатуры целочисленное значение
y =  int(input("Минут в пути: ")) #ввод с клавиатуры целочисленное значение
print(f'В пути Доктор Иванов проводит {(x*60 + y)*2} минут')  #вывод произмедения значений

#7
print("\nЗадание 7")
a = False
b = True
c = False
print((not a or b) and c)

#8
print("\nЗадание 8")
year = int(input("Введите год: "))  #ввод года рождения
if year > 1900 or year < 3000: #условие чтобы задать ограничение на ввод чисел от 1900 года до 3000 года
    if  year % 4 == 0 and year % 100 != 0 or year % 400 == 0: #условие для проверки является ли год высококосным или нет
        print('С днём рождения!') #вывод в случае год является высококосным
    else:
        print('Год обычный') #в ином случае выводится соответствующее сообщение о том, что год не является высококосным
else:
    print('Год не входит в выборку') #вывод сообщения о том, что введенное значение вышла за диапазон

#9
print("Задание 9\n")
x = 0 #начальное значение для цикла
while x <= 20: #цикл начинается с 0 до 20го
    if x % 2 == 0: #условие на проверку четное или нечетное число
        print(x, end=' ') #вывод значения
    x += 1 #увеличение значения х на 1

#10
print("\nЗадание 10")
total_sum = 0 #переменная для подсчета суммы
number = int(input("Введите число: ")) #ввод чисел
while number != 0: #цикл продолжается до тех пор, пока не будет введено 0
    total_sum += number #суммируем каждый элемент
    number = int(input("Введите число: ")) #вводим число вннтри цикла
print("Итоговая сумма:", total_sum) #вывод итоговый суммы

#11
print("\nЗадание 11")
X = int(input("Введите количество коллег в клинике: "))
Y = int(input("Введите количество коллег в поликлинике: "))

if X > Y: #условие для проверки какое из значений больше
    max_peices = X #присваиваем переменной значение Х
else:
    max_peices = Y #присваиваем переменной значение У

while (True):
    if ((max_peices % X == 0) and (max_peices % Y == 0)):
        lcm = max_peices
        break
    max_peices += 1
nok = lcm
print(f"Нужно кусков: {X} и {Y}: {nok}")

#12
print("\nЗадание 12")
for i in range(2, 21, 2): #цикл начальное число в цикле 2 до 21го элемента с шагом 2
    print(i, end=" ") #вывод

#13
print("\nЗадание 13")
a, b, c, d = [int(input()) for i in range(4)]
print('', end='\t')
for j in range(c, d + 1):
    print(j, end='\t')
print()
for i in range(a, b + 1):
    print(i, end='\t')
    for j in range(c, d + 1):
        print(i * j, end='\t')
    print()

#14
print("\nЗадание 14")
#n - размерность матрицы n x n
#mat - результирующая матрица
#st - текущее значение-счетчик для записи в матрицу
#m - коеффициент, используемый для заполнения верхней
#матрицы последующих витков, т.к. одномерные матрицы
#следующих витков имеют меньше значений
n = int(input("Введите размерность квадратной матрицы: "))
mat = [[0]*n for i in range(n)]
st, m = 1, 0
# Заранее присваиваю значение центральному элементу
# матрицы
mat[n//2][n//2]=n*n
for v in range(n//2):
    #Заполнение верхней горизонтальной матрицы
    for i in range(n-m):
        mat[v][i+v] = st
        st+=1
        #i+=1
    #Заполнение правой вертикальной матрицы
    for i in range(v+1, n-v):
        mat[i][-v-1] = st
        st+=1
        #i+=1
    #Заполнение нижней горизонтальной матрицы
    for i in range(v+1, n-v):
        mat[-v-1][-i-1] =st
        st+=1
        #i+=1
    #Заполнение левой вертикальной матрицы
    for i in range(v+1, n-(v+1)):
        mat[-i-1][v]=st
        st+=1
        #i+=1
    #v+=1
    m+=2
#Вывод результата на экран
for i in mat:
    print(*i)
